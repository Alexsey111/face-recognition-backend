# 🛠️ Инструменты для решения проблем с виртуальной средой

## 📦 Созданные инструменты

Мы создали полный набор инструментов для диагностики и решения проблем с виртуальной средой Face Recognition Service:

### 1. 📚 Документация
- **VENV_SETUP.md** - Подробное руководство по настройке виртуальной среды
- **QUICK_START_VENV.md** - Быстрое решение основных проблем
- **PROJECT_STRUCTURE.md** - Полная структура проекта

### 2. 🔧 Автоматические скрипты
- **setup_venv.sh** - Автоматическая настройка для Linux/Mac
- **setup_venv.bat** - Автоматическая настройка для Windows
- **diagnose_env.py** - Скрипт диагностики проблем

### 3. 📋 Управление проектом
- **Makefile** - Набор команд для управления проектом

## 🚀 Быстрый старт

### Автоматическая настройка
```bash
# Linux/Mac
./setup_venv.sh

# Windows  
setup_venv.bat

# Или используя Makefile
make setup
```

### Диагностика проблем
```bash
# Общая диагностика
python diagnose_env.py

# Проверка статуса
make status
```

### Решение конкретных проблем

#### Python версия
```bash
python diagnose_env.py | grep "Python"
```

#### Зависимости
```bash
make install-dev
python diagnose_env.py | grep "Зависимост"
```

#### Запуск приложения
```bash
make dev  # Development режим
make run  # Production режим
```

## 🛠️ Доступные команды Makefile

```bash
make help          # Показать все команды
make setup         # Настроить виртуальную среду
make install       # Установить production зависимости
make install-dev   # Установить все зависимости
make dev           # Запустить в development режиме
make test          # Запустить тесты
make lint          # Проверить код линтерами
make format        # Форматировать код
make clean         # Очистить временные файлы
make docker-up     # Запустить через Docker
```

## 🔍 Диагностика

### Автоматическая диагностика
```bash
python diagnose_env.py
```

### Ручная проверка
```bash
# Проверка Python
python --version

# Проверка виртуальной среды
echo $VIRTUAL_ENV

# Проверка зависимостей
python -c "import fastapi; print('OK')"

# Запуск тестов
make test
```

## 🐳 Docker (альтернатива)

Если проблемы с виртуальной средой продолжаются:

```bash
make docker-up      # Запуск всех сервисов
make docker-up-dev  # Development режим
make docker-logs    # Просмотр логов
```

## 🆘 Частые проблемы

### 1. Python не найден
**Симптомы:** `python: command not found`
**Решение:** Установите Python 3.11+ с официального сайта

### 2. Версия Python старая
**Симптомы:** Требуется Python 3.11+
**Решение:** 
```bash
# Ubuntu/Debian
sudo apt install python3.11 python3.11-venv python3.11-dev
```

### 3. OpenCV не устанавливается
**Симптомы:** Ошибки компиляции OpenCV
**Решение:**
```bash
# Ubuntu/Debian
sudo apt install libgl1-mesa-glx libglib2.0-0
pip install opencv-python-headless
```

### 4. psycopg2 ошибки
**Симптомы:** Ошибки установки PostgreSQL драйвера
**Решение:**
```bash
# Ubuntu/Debian
sudo apt install libpq-dev
pip install psycopg2-binary
```

### 5. Проблемы с правами
**Симптомы:** `Permission denied`
**Решение:** Создайте venv без sudo:
```bash
python3 -m venv venv --without-pip
python3 -m ensurepip --default-pip
```

## 📊 Статистика созданных файлов

| Файл | Тип | Размер | Назначение |
|------|-----|--------|------------|
| VENV_SETUP.md | Документация | ~8KB | Подробное руководство |
| QUICK_START_VENV.md | Документация | ~3KB | Быстрое решение |
| setup_venv.sh | Скрипт Linux/Mac | ~8KB | Автоматическая настройка |
| setup_venv.bat | Скрипт Windows | ~3KB | Автоматическая настройка |
| diagnose_env.py | Скрипт Python | ~6KB | Диагностика проблем |
| Makefile | Конфигурация | ~15KB | Управление проектом |

**Всего:** 6 файлов, ~43KB

## ✅ Чек-лист для пользователя

- [ ] Прочитать QUICK_START_VENV.md
- [ ] Запустить диагностику: `python diagnose_env.py`
- [ ] Выполнить автоматическую настройку: `./setup_venv.sh` или `make setup`
- [ ] Проверить установку: `make test`
- [ ] Запустить приложение: `make dev`
- [ ] При проблемах использовать Docker: `make docker-up`

## 🎯 Следующие шаги

1. **Для разработки:** `make dev`
2. **Для тестирования:** `make test`
3. **Для production:** `make deploy`
4. **Для Docker:** `make docker-up`

---

## 📞 Получение помощи

- **Быстрая помощь:** `make help`
- **Диагностика:** `python diagnose_env.py`
- **Документация:** README.md, VENV_SETUP.md
- **Структура проекта:** PROJECT_STRUCTURE.md

Все инструменты готовы к использованию! 🎉