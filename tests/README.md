# Тестирование Face Recognition Service

Этот документ описывает структуру тестов и способы их запуска для проекта Face Recognition Service.

## 📁 Структура тестов

```
tests/
├── __init__.py                 # Инициализация пакета тестов
├── conftest.py                 # Общие фикстуры для pytest
├── unit/                       # Юнит-тесты
│   ├── __init__.py
│   ├── test_config.py          # Тесты конфигурации
│   ├── test_validators.py      # Тесты валидаторов
│   ├── test_services.py        # Тесты сервисов
│   ├── test_models.py          # Тесты моделей данных
│   ├── test_database.py        # Тесты базы данных
│   └── test_utils.py           # Тесты утилит
└── integration/                # Интеграционные тесты
    ├── __init__.py
    └── test_api_endpoints.py   # Тесты API endpoints
```

## 🧪 Типы тестов

### Юнит-тесты (`tests/unit/`)
Быстрые тесты отдельных компонентов без внешних зависимостей:
- **test_config.py** - тестирование конфигурации приложения
- **test_validators.py** - тестирование валидаторов входных данных
- **test_services.py** - тестирование бизнес-логики сервисов
- **test_models.py** - тестирование Pydantic моделей
- **test_database.py** - тестирование CRUD операций
- **test_utils.py** - тестирование вспомогательных функций

### Интеграционные тесты (`tests/integration/`)
Тестирование взаимодействия между компонентами:
- **test_api_endpoints.py** - тестирование REST API endpoints

## 🚀 Запуск тестов

### Способы запуска

#### 1. Автоматический скрипт (рекомендуется)
```bash
python run_tests.py
```

#### 2. Ручной запуск через pytest

**Все тесты:**
```bash
python -m pytest tests/ -v
```

**Только юнит-тесты:**
```bash
python -m pytest tests/unit/ -v
```

**Только интеграционные тесты:**
```bash
python -m pytest tests/integration/ -v
```

**С покрытием кода:**
```bash
python -m pytest tests/ --cov=app --cov-report=term-missing
```

**С HTML отчетом:**
```bash
python -m pytest tests/ --cov=app --cov-report=html:htmlcov
```

**По маркерам:**
```bash
# Только быстрые тесты
python -m pytest -m "unit" -v

# Только тесты API
python -m pytest -m "api" -v

# Исключая медленные тесты
python -m pytest -m "not slow" -v
```

### Docker запуск (как в проекте)

**Все тесты:**
```bash
docker-compose -f docker-compose.test.yml up --abort-on-container-exit
```

**Конкретный тип тестов:**
```bash
docker-compose -f docker-compose.test.yml exec face-recognition-api-test pytest tests/unit -v
```

## 📊 Покрытие кода

Проект настроен на достижение минимум 80% покрытия кода. Отчеты генерируются в:
- `htmlcov/index.html` - HTML отчет
- `coverage.xml` - XML отчет для CI/CD

## 🔧 Настройки pytest

Основные настройки находятся в `pytest.ini`:
- Пути поиска тестов
- Формат вывода
- Маркеры для группировки
- Фильтрация предупреждений
- Переменные окружения для тестов

## 📋 Маркеры тестов

Используемые маркеры для группировки тестов:

- `unit` - юнит-тесты (быстрые)
- `integration` - интеграционные тесты
- `slow` - медленные тесты
- `api` - тесты API endpoints
- `database` - тесты базы данных
- `ml` - тесты машинного обучения
- `security` - тесты безопасности
- `performance` - тесты производительности
- `smoke` - smoke тесты
- `regression` - регрессионные тесты
- `auth` - тесты аутентификации

## 🛠 Зависимости для тестирования

Тесты используют следующие библиотеки (из `requirements-dev.txt`):

```txt
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0
httpx==0.25.2
```

## 🔍 Примеры использования

### Запуск конкретного теста
```bash
python -m pytest tests/unit/test_config.py::TestConfig::test_config_creation -v
```

### Запуск тестов с определенным маркером
```bash
python -m pytest -m "unit and not slow" -v
```

### Запуск с детальным выводом
```bash
python -m pytest tests/ -v -s --tb=long
```

### Параллельный запуск (если установлен pytest-xdist)
```bash
python -m pytest tests/ -n auto
```

## 📈 Интерпретация результатов

### Коды возврата
- `0` - все тесты прошли успешно
- `1` - один или более тестов не прошли
- `2` - прервано пользователем
- `3` - внутренняя ошибка
- `4` - ошибка в командной строке pytest
- `5` - не найдены тесты

### Формат вывода
- `.` - тест прошел успешно
- `F` - тест не прошел (FAILED)
- `E` - ошибка в тесте (ERROR)
- `x` - тест был пропущен (SKIPPED)

## 🚨 Устранение проблем

### Проблемы с зависимостями
```bash
pip install -r requirements-dev.txt
```

### Проблемы с pytest-asyncio
Убедитесь, что в `pytest.ini` установлено:
```ini
asyncio_mode = auto
```

### Проблемы с базой данных
Для интеграционных тестов требуется настроенная тестовая база данных.

### Проблемы с покрытием кода
Убедитесь, что установлен `pytest-cov`:
```bash
pip install pytest-cov
```

## 📞 Поддержка

Если у вас возникли проблемы с тестами:
1. Проверьте, что все зависимости установлены
2. Убедитесь, что переменные окружения настроены правильно
3. Проверьте логи ошибок в выводе pytest
4. Обратитесь к документации pytest: https://docs.pytest.org/