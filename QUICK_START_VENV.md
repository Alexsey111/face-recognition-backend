# 🚀 Быстрый старт - Решение проблем с виртуальной средой

## 📋 Быстрое решение проблем

Если у вас возникли проблемы с виртуальной средой, выполните следующие шаги:

### 1. Автоматическая настройка

**Linux/Mac:**
```bash
./setup_venv.sh
```

**Windows:**
```cmd
setup_venv.bat
```

### 2. Использование Makefile

**Показать все доступные команды:**
```bash
make help
```

**Настроить среду:**
```bash
make setup
```

**Установить зависимости:**
```bash
make install-dev
```

**Запустить приложение:**
```bash
make dev
```

**Запустить тесты:**
```bash
make test
```

## 🔧 Ручная настройка

Если автоматический скрипт не работает:

### Создание виртуальной среды
```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

### Установка зависимостей
```bash
pip install --upgrade pip setuptools wheel
pip install -r requirements.txt
```

### Проверка установки
```bash
python -c "import fastapi; print('OK')"
```

## 🐳 Docker (альтернатива)

Если проблемы продолжаются, используйте Docker:

```bash
# Запуск в Docker
make docker-up

# Development режим
make docker-up-dev

# Логи
make docker-logs
```

## 🆘 Частые проблемы

### Ошибка: Python не найден
**Решение:** Установите Python 3.11+ с официального сайта

### Ошибка: OpenCV не устанавливается
**Решение:** 
```bash
sudo apt install libgl1-mesa-glx libglib2.0-0  # Linux
pip install opencv-python-headless
```

### Ошибка: psycopg2 не устанавливается
**Решение:**
```bash
sudo apt install libpq-dev  # Linux
pip install psycopg2-binary
```

### Ошибка: Permission denied
**Решение:** Создайте venv без sudo:
```bash
python3 -m venv venv --without-pip
python3 -m ensurepip --default-pip
```

## 📚 Дополнительная документация

- **VENV_SETUP.md** - подробное руководство
- **Makefile** - все доступные команды
- **README.md** - общая документация проекта

## ✅ Чек-лист настройки

- [ ] Python 3.11+ установлен
- [ ] Виртуальная среда создана
- [ ] Зависимости установлены
- [ ] Тесты проходят
- [ ] Приложение запускается

## 🎯 Следующие шаги

1. Настройте переменные окружения в `.env`
2. Запустите `make test` для проверки
3. Запустите `make dev` для разработки
4. Изучите API документацию на `/docs`

---

**Если проблемы остались:** Проверьте `VENV_SETUP.md` для подробных инструкций.