# Архитектура Face Recognition Service

## 📋 Содержание

1. [Общий обзор](#общий-обзор)
2. [Архитектурные паттерны](#архитектурные-паттерны)
3. [Слои приложения](#слои-приложения)
4. [Компоненты системы](#компоненты-системы)
5. [Схема взаимодействия](#схема-взаимодействия)
6. [Технологический стек](#технологический-стек)
7. [Безопасность](#безопасность)
8. [Масштабирование](#масштабирование)

---

## Общий обзор

Face Recognition Service — это высокопроизводительный микросервис для распознавания лиц, построенный на принципах Clean Architecture и hexagonal architecture. Сервис обеспечивает:

- **Обнаружение лиц** в изображениях
- **Извлечение векторных представлений** (embeddings)
- **Верификацию личности** по эталонным изображениям
- **Liveness Detection** — определение живой личности vs фотографии
- **Batch processing** — массовую обработку изображений

### Ключевые характеристики

| Характеристика | Значение |
|----------------|----------|
| Время отклика (P95) | < 100 мс |
| Пропускная способность | 1000+ запросов/мин |
| Доступность | 99.9% |
| Масштабирование | Горизонтальное |

---

## Архитектурные паттерны

### 1. Clean Architecture

Проект следует принципам Clean Architecture с чётким разделением ответственности:

```
┌─────────────────────────────────────────────────────────────┐
│                    Внешний слой (Frameworks)                 │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐   │
│  │   FastAPI │ │  Docker   │ │  PostgreSQL│ │   Redis   │   │
│  └───────────┘ └───────────┘ └───────────┘ └───────────┘   │
├─────────────────────────────────────────────────────────────┤
│                  Слой адаптеров (Adapters)                   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Routes (API)  │  Services (Business Logic)         │   │
│  └─────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│                    Слой приложения (Use Cases)               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Services (Anti-spoofing, ML, Auth, Storage...)     │   │
│  └─────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│                    Слой домена (Domain)                      │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐   │
│  │  Models   │ │  Config   │ │  Utils    │ │  Middleware│   │
│  └───────────┘ └───────────┘ └───────────┘ └───────────┘   │
├─────────────────────────────────────────────────────────────┤
│                    Внутренний слой (Enterprise)              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Database Models │  Business Rules │  Entities      │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 2. MVC Pattern

- **Models**: Pydantic модели для валидации данных
- **Views**: FastAPI routes/endpoints
- **Controllers**: Services (бизнес-логика)

### 3. Repository Pattern

Абстракция доступа к данным через слой `services/` с конкретными реализациями в `db/`.

---

## Слои приложения

### 1. HTTP/Routes Layer (`app/routes/`)

API endpoints, обрабатывающие HTTP запросы:

| Файл | Ответственность |
|------|-----------------|
| `admin.py` | Административные функции, статистика |
| `auth.py` | Аутентификация и авторизация |
| `health.py` | Health checks |
| `liveness.py` | Liveness detection endpoints |
| `metrics.py` | Prometheus метрики |
| `reference.py` | CRUD эталонных изображений |
| `upload.py` | Загрузка файлов |
| `verify.py` | Верификация личности |
| `webhook.py` | Webhook endpoints |

### 2. Business Logic Layer (`app/services/`)

Бизнес-логика приложения:

| Сервис | Ответственность |
|--------|-----------------|
| `anti_spoofing_service.py` | Определение spoof-атак |
| `auth_service.py` | JWT аутентификация |
| `cache_service.py` | Кэширование в Redis |
| `database_service.py` | Операции с БД |
| `encryption_service.py` | Шифрование данных |
| `ml_service.py` | ML inference |
| `session_service.py` | Управление сессиями |
| `storage_service.py` | Работа с S3/MinIO |
| `validation_service.py` | Валидация данных |
| `webhook_service.py` | Webhook уведомления |

### 3. Middleware Layer (`app/middleware/`)

HTTP middleware компоненты:

| Компонент | Функция |
|-----------|---------|
| `auth.py` | JWT валидация |
| `cors.py` | CORS политики |
| `error_handler.py` | Глобальная обработка ошибок |
| `logging.py` | Структурированное логирование |
| `metrics.py` | Prometheus метрики |
| `rate_limit.py` | Rate limiting |
| `request_logging.py` | Логирование запросов |

### 4. Utils Layer (`app/utils/`)

Вспомогательные функции и утилиты:

| Файл | Назначение |
|------|------------|
| `constants.py` | Константы приложения |
| `decorators.py` | Кастомные декораторы |
| `depth_estimator.py` | Оценка глубины для liveness |
| `exceptions.py` | Кастомные исключения |
| `face_aligner.py` | Выравнивание лиц |
| `face_alignment_utils.py` | Утилиты выравнивания |
| `file_utils.py` | Работа с файлами |
| `helpers.py` | Вспомогательные функции |
| `lighting_analyzer.py` | Анализ освещения |
| `logger.py` | Настройка логирования |
| `security.py` | Функции безопасности |
| `structured_logging.py` | Структурированный вывод |
| `validators.py` | Валидаторы данных |

### 5. Database Layer (`app/db/`)

Работа с базой данных:

| Файл | Ответственность |
|------|-----------------|
| `crud.py` | CRUD операции |
| `database.py` | Соединение с БД |
| `models.py` | SQLAlchemy модели |

### 6. Configuration Layer (`app/config.py`)

Централизованная конфигурация через Pydantic Settings:

```python
# Пример использования
from app.config import Settings
settings = Settings()
```

---

## Компоненты системы

### 1. Face Detection & Embedding

```
┌─────────────┐     ┌──────────────────┐     ┌───────────────┐
│   Загрузка  │────▶│  Preprocessing   │────▶│    Face       │
│   изображения│     │  (Normalization) │     │   Detection   │
└─────────────┘     └──────────────────┘     └───────┬───────┘
                                                      │
┌─────────────┐     ┌──────────────────┐             │
│   Ответ     │◀────│  Embedding       │◀────────────┘
│   (Vector)  │     │  Extraction      │
└─────────────┘     └──────────────────┘
```

### 2. Liveness Detection

```
┌─────────────┐     ┌──────────────────┐     ┌───────────────┐
│   Изображение│────▶│  Anti-Spoofing   │────▶│    Результат  │
│             │     │  Service         │     │  (Live/Spoof) │
└─────────────┘     └──────────────────┘     └───────────────┘
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
        ┌──────────┐ ┌──────────┐ ┌──────────┐
        │ Depth    │ │ Lighting │ │ Texture  │
        │ Analysis │ │ Analysis │ │ Analysis │
        └──────────┘ └──────────┘ └──────────┘
```

### 3. Verification Flow

```
┌─────────────┐     ┌──────────────────┐     ┌───────────────┐
│   Query     │────▶│  Embedding       │────▶│    Cosine     │
│   Image     │     │  Extraction      │     │   Similarity  │
└─────────────┘     └──────────────────┘     └───────┬───────┘
                                                      │
              ┌───────────────────────────────────────┘
              ▼
┌─────────────┐     ┌──────────────────┐     ┌───────────────┐
│   Reference │────▶│  Embedding       │────▶│    Decision   │
│   Image     │     │  Extraction      │     │  (Match/Not)  │
└─────────────┘     └──────────────────┘     └───────────────┘
```

---

## Схема взаимодействия

### HTTP Request Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                        Client Request                            │
└─────────────────────────────┬───────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      FastAPI Application                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │   CORS      │  │   Rate      │  │   Auth      │             │
│  │  Middleware │  │   Limit     │  │  Middleware │             │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘             │
│         │                │                │                     │
│         ▼                ▼                ▼                     │
│  ┌─────────────────────────────────────────────────────┐       │
│  │                   Router                             │       │
│  │   ┌─────────────────────────────────────────────┐   │       │
│  │   │              Route Handler                   │   │       │
│  │   └─────────────────────┬───────────────────────┘   │       │
│  │                         ▼                           │       │
│  │   ┌─────────────────────────────────────────────┐   │       │
│  │   │              Service Layer                   │   │       │
│  │   │   (Auth, ML, Storage, Validation...)        │   │       │
│  │   └─────────────────────┬───────────────────────┘   │       │
│  │                         ▼                           │       │
│  │   ┌─────────────────────────────────────────────┐   │       │
│  │   │              Repository Layer               │   │       │
│  │   │         (Database, Cache, Storage)          │   │       │
│  │   └─────────────────────────────────────────────┘   │       │
│  └─────────────────────────────────────────────────────┘       │
└─────────────────────────────────────────────────────────────────┘
```

### Database Schema

```
┌─────────────────────────────────────────────────────────────┐
│                      PostgreSQL                              │
├─────────────────────────────────────────────────────────────┤
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐   │
│  │    users      │  │  references   │  │  verifications │   │
│  ├───────────────┤  ├───────────────┤  ├───────────────┤   │
│  │ id (PK)       │  │ id (PK)       │  │ id (PK)        │   │
│  │ email         │  │ user_id (FK)  │  │ user_id (FK)   │   │
│  │ password_hash │  │ embedding     │  │ reference_id   │   │
│  │ role          │  │ image_url     │  │ similarity     │   │
│  │ is_active     │  │ label         │  │ is_match       │   │
│  │ created_at    │  │ created_at    │  │ created_at     │   │
│  └───────────────┘  └───────────────┘  └───────────────┘   │
│  ┌───────────────┐  ┌───────────────┐                      │
│  │    webhooks   │  │    sessions   │                      │
│  ├───────────────┤  ├───────────────┤                      │
│  │ id (PK)       │  │ id (PK)       │                      │
│  │ url           │  │ user_id (FK)  │                      │
│  │ events        │  │ token         │                      │
│  │ secret        │  │ expires_at    │                      │
│  │ is_active     │  │ created_at    │                      │
│  └───────────────┘  └───────────────┘                      │
└─────────────────────────────────────────────────────────────┘
```

---

## Технологический стек

### Backend

| Компонент | Технология | Версия |
|-----------|------------|--------|
| Runtime | Python | 3.11+ |
| Framework | FastAPI | 0.109+ |
| ASGI Server | Uvicorn | Latest |
| Database | PostgreSQL | 14+ |
| ORM | SQLAlchemy | 2.0+ |
| Migration | Alembic | Latest |
| Validation | Pydantic | 2.0+ |
| Caching | Redis | 7.0+ |
| Object Storage | MinIO/S3 | Latest |

### DevOps

| Компонент | Технология |
|-----------|------------|
| Containerization | Docker |
| Orchestration | Docker Compose |
| CI/CD | GitHub Actions |
| Monitoring | Prometheus + Grafana |
| Logging | Structured JSON logs |

### ML/AI

| Компонент | Технология |
|-----------|------------|
| Face Detection | Deep Learning Model |
| Embedding Extraction | Neural Network |
| Liveness Detection | Multi-modal Analysis |

### Security

| Компонент | Технология |
|-----------|------------|
| Authentication | JWT (PyJWT) |
| Password Hashing | Bcrypt |
| Encryption | AES-256-GCM |
| Rate Limiting | SlowAPI + Redis |

---

## Безопасность

### Authentication Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                      JWT Authentication                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   Client ─────▶ POST /api/v1/auth/login ─────▶ Auth Service    │
│                     (email + password)                           │
│                                                                 │
│   Auth Service ─────▶ Validate Credentials ─────▶ DB Check     │
│                                                                 │
│   DB Check ─────▶ User Found + Password Match ─────▶ ✅        │
│                                                                 │
│   Auth Service ─────▶ Generate JWT Token ─────▶ Response       │
│                                                                 │
│   Client ─────▶ Request with JWT Header ─────▶ Middleware      │
│                                                                 │
│   Middleware ─────▶ Validate Token ─────▶ Extract Claims       │
│                                                                 │
│   Claims ─────▶ Check Permissions ─────▶ Route Handler         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Security Features

1. **Аутентификация**
   - JWT токены с expiration
   - Refresh token механизм
   - API ключи для сервисов

2. **Авторизация**
   - RBAC (Role-Based Access Control)
   - Permission-based endpoints

3. **Шифрование**
   - AES-256-GCM для чувствительных данных
   - TLS для транспорта

4. **Защита от атак**
   - Rate limiting (Redis-backed)
   - CORS policies
   - SQL injection protection (SQLAlchemy)
   - XSS protection

---

## Масштабирование

### Горизонтальное масштабирование

```
                         ┌─────────────────┐
                         │    Load         │
                         │    Balancer     │
                         │   (Nginx)       │
                         └────────┬────────┘
                                  │
         ┌────────────────────────┼────────────────────────┐
         │                        │                        │
         ▼                        ▼                        ▼
   ┌──────────┐            ┌──────────┐            ┌──────────┐
   │   API    │            │   API    │            │   API    │
   │ Instance │            │ Instance │            │ Instance │
   │    1     │            │    2     │            │    N     │
   └────┬─────┘            └────┬─────┘            └────┬─────┘
        │                       │                       │
        └───────────────────────┼───────────────────────┘
                                │
                    ┌───────────┴───────────┐
                    │                       │
                    ▼                       ▼
            ┌─────────────┐         ┌─────────────┐
            │  PostgreSQL │         │    Redis    │
            │  (Primary)  │         │   Cluster   │
            └─────────────┘         └─────────────┘
```

### Connection Pooling

```python
# Database connection pool
DATABASE_POOL_SIZE = 10
DATABASE_MAX_OVERFLOW = 20

# Redis connection pool
REDIS_CONNECTION_POOL_SIZE = 10
```

### Caching Strategy

```
┌─────────────────────────────────────────────────────────────┐
│                     Caching Layers                           │
├─────────────────────────────────────────────────────────────┤
│                                                                 │
│   Level 1: In-Memory Cache (per process)                      │
│   └── LRU cache для frequently accessed embeddings           │
│                                                                 │
│   Level 2: Redis Cache (distributed)                          │
│   └── Session data, rate limits, API responses                │
│                                                                 │
│   Level 3: CDN (static assets)                                │
│   └── Reference images, thumbnails                            │
│                                                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## Структура файлов

```
face-recognition-service/
└── app/
    └── models/
        ├── __init__.py                 # Инициализация пакета моделей
        │
        ├── face.py                     # Модели для распознавания лиц
        │   ├── FaceDetectionResult     # Результат детекции лица
        │   ├── FaceLandmarks           # Ключевые точки лица
        │   ├── FaceEmbedding           # Эмбеддинг лица
        │   ├── FaceEmbeddingComparison # Сравнение эмбеддингов
        │   ├── FaceQualityAssessment   # Оценка качества лица
        │   ├── LightingAnalysis        # Анализ освещения
        │   ├── DepthAnalysis           # Анализ глубины (3D)
        │   ├── LivenessDetectionResult # Проверка живости
        │   ├── ActiveLivenessChallenge # Активный челлендж
        │   ├── FaceAlignmentResult     # Выравнивание лица
        │   ├── ComprehensiveFaceAnalysis  # Полный анализ
        │   ├── BatchFaceProcessingRequest # Пакетная обработка
        │   ├── BatchFaceProcessingResult  # Результат пакета
        │   └── SpoofingIndicators      # Индикаторы спуфинга
        │
        ├── reference.py                # Модели для эталонов
        │   ├── ReferenceModel          # Базовый эталон
        │   ├── ReferenceCreate         # Создание эталона
        │   ├── ReferenceUpdate         # Обновление эталона
        │   ├── ReferenceSearch         # Поиск эталонов
        │   ├── ReferenceCompare        # Сравнение с эталонами
        │   ├── ReferenceCompareResult  # Результат сравнения
        │   ├── ReferenceListResponse   # Список эталонов
        │   └── ReferenceStats          # Статистика эталонов
        │
        ├── request.py                  # Модели запросов
        │   ├── UploadRequest           # Загрузка изображения
        │   ├── VerifyRequest           # Запрос верификации
        │   ├── LivenessRequest         # Проверка живости
        │   ├── VideoLivenessRequest    # Видео проверка живости
        │   ├── BatchEmbeddingRequest   # Пакетный эмбеддинг
        │   ├── BatchVerificationRequest# Пакетная верификация
        │   ├── AdvancedAntiSpoofingRequest
        │   ├── ReferenceCreateRequest  # Создание эталона
        │   ├── ReferenceUpdateRequest  # Обновление эталона
        │   ├── AdminStatsRequest       # Статистика админа
        │   ├── TokenRefreshRequest     # Обновление токена
        │   ├── UserCreate/UserLogin    # Создание/логин пользователя
        │   └── CompareRequest          # Запрос сравнения
        │
        ├── response.py                 # Модели ответов
        │   ├── BaseResponse            # Базовый ответ
        │   ├── ErrorResponse           # Ответ с ошибкой
        │   ├── UploadResponse          # Ответ загрузки
        │   ├── VerifyResponse          # Ответ верификации
        │   ├── LivenessResponse        # Ответ живости
        │   ├── VideoLivenessResponse   # Видео живость
        │   ├── BatchEmbeddingResponse  # Пакет эмбеддингов
        │   ├── BatchVerificationResponse
        │   ├── AdvancedAntiSpoofingResponse
        │   ├── ChallengeResponse       # Challenge-response
        │   ├── ActiveLivenessResponse  # Активная живость
        │   ├── ReferenceResponse       # Ответ эталона
        │   ├── ReferenceListResponse   # Список эталонов
        │   ├── HealthResponse          # Проверка здоровья
        │   ├── StatusResponse          # Детальный статус
        │   ├── AdminStatsResponse      # Статистика админа
        │   ├── AuthResponse            # Ответ аутентификации
        │   ├── TokenResponse           # Ответ токена
        │   ├── SessionResponse         # Ответ сессии
        │   └── PaginatedResponse       # Пагинированный ответ
        │
        ├── user.py                     # Модели пользователя
        │   ├── UserModel               # Базовый пользователь
        │   ├── UserCreate              # Создание пользователя
        │   ├── UserUpdate              # Обновление пользователя
        │   ├── UserResponse            # Ответ пользователя
        │   └── UserListResponse        # Список пользователей
        │
        ├── verification.py             # Модели верификации
        │   ├── VerificationSessionModel
        │   ├── VerificationSessionCreate
        │   ├── VerificationSessionUpdate
        │   ├── VerificationRequest     # Запрос верификации
        │   ├── VerificationResult      # Результат верификации
        │   ├── LivenessResult          # Результат живости
        │   ├── SessionListResponse     # Список сессий
        │   ├── SessionStats            # Статистика сессий
        │   └── SessionSearch           # Поиск сессий
        │
        └── webhook.py                  # Модели webhook
            ├── WebhookEventType        # Типы событий
            ├── WebhookStatus           # Статусы webhook
            ├── WebhookConfigBase/Create/Update/Response
            ├── WebhookLogBase/Create/Update/Response
            ├── WebhookPayload          # Payload webhook
            ├── WebhookVerificationData # Данные верификации
            ├── WebhookLivenessData     # Данные живости
            ├── WebhookTestRequest/Response
            ├── WebhookStatistics       # Статистика
            ├── WebhookRetryRequest     # Повторная отправка
            └── WebhookBulkAction       # Массовые операции
```

---

## Best Practices

### 1. Кодовая база

- **Type hints** для всех функций и методов
- **Async/await** для I/O операций
- **Dependency injection** через FastAPI Depends
- **Error handling** с кастомными исключениями

### 2. Тестирование

- **Unit tests** для каждого сервиса
- **Integration tests** для API endpoints
- **E2E tests** для критических путей
- **Coverage** не менее 80%

### 3. CI/CD

- **Автоматические тесты** на каждый PR
- **Linting** (black, isort, flake8, mypy)
- **Security scans** (bandit, safety)
- **Docker builds** с multi-stage builds

### 4. Мониторинг

- **Health checks** для всех сервисов
- **Metrics** в Prometheus формате
- **Structured logging** в JSON
- **Distributed tracing** (опционально)

---

## Ссылки

- [README.md](README.md) — Общее описание проекта
- [API.md](API.md) — Документация API
- [DEPLOYMENT.md](DEPLOYMENT.md) — Гайд по деплою
- [DEVELOPMENT.md](DEVELOPMENT.md) — Гайд по разработке
- [SECURITY.md](SECURITY.md) — Безопасность
- [docs/PERFORMANCE.md](docs/PERFORMANCE.md) — Оптимизация производительности