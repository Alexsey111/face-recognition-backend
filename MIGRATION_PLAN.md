# План миграции на архитектуру из спецификации

## 🎯 Цель
Привести проект к полному соответствию описанной архитектуре с использованием SCRFD, ArcFace, ONNX Runtime и Silent-Face-Anti-Spoofing.

---

## 📋 ЭТАП 1: Замена моделей ML (2-3 недели)

### 1.1 Интеграция SCRFD для детекции

**Текущее состояние:**
- Используется MTCNN (facenet-pytorch)
- PyTorch инференс

**Целевое состояние:**
- SCRFD детектор
- ONNX Runtime инференс

**Шаги:**
1. Установить зависимости:
   ```bash
   pip install onnxruntime-gpu  # или onnxruntime для CPU
   pip install insightface
   ```

2. Скачать SCRFD модель:
   ```python
   # Пример интеграции
   from insightface import app as face_app
   from insightface.model_zoo import model_zoo
   
   # SCRFD модель
   detector = model_zoo.get_model('scrfd_500m_bnkps')
   detector.prepare(ctx_id=0, input_size=(640, 640))
   ```

3. Конвертировать в ONNX (если нужно):
   ```python
   # Конвертация PyTorch -> ONNX
   torch.onnx.export(model, dummy_input, "scrfd.onnx")
   ```

4. Обновить `MLService`:
   - Заменить MTCNN на SCRFD
   - Использовать ONNX Runtime для инференса
   - Сохранить совместимость API

5. Тестирование:
   - Проверить точность детекции
   - Проверить производительность (<150 мс)
   - Обновить unit тесты

**Файлы для изменения:**
- `app/services/ml_service.py` - основной сервис
- `app/config.py` - настройки моделей
- `requirements.txt` - зависимости

### 1.2 Интеграция ArcFace для распознавания

**Текущее состояние:**
- Используется FaceNet (InceptionResnetV1)
- 512-мерные эмбеддинги

**Целевое состояние:**
- ArcFace (InsightFace)
- 512-мерные эмбеддинги
- ONNX Runtime

**Шаги:**
1. Установить InsightFace:
   ```bash
   pip install insightface
   ```

2. Загрузить ArcFace модель:
   ```python
   from insightface import app as face_app
   
   # ArcFace модель
   recognizer = face_app.FaceAnalysis(providers=['CUDAExecutionProvider'])
   recognizer.prepare(ctx_id=0, det_size=(640, 640))
   ```

3. Конвертировать в ONNX:
   ```python
   # Конвертация для оптимизации
   # Использовать onnxruntime для инференса
   ```

4. Обновить `generate_embedding()`:
   - Заменить FaceNet на ArcFace
   - Сохранить формат вывода (512-мерный вектор)
   - Использовать ONNX Runtime

5. Тестирование:
   - Проверить точность на LFW, AgeDB
   - Проверить производительность
   - Обновить тесты

**Файлы для изменения:**
- `app/services/ml_service.py` - метод `generate_embedding()`
- `app/services/ml_service.py` - метод `_generate_face_embedding()`

---

## 📋 ЭТАП 2: Интеграция Silent-Face-Anti-Spoofing (1-2 недели)

### 2.1 Установка и интеграция модели

**Текущее состояние:**
- Эвристическая реализация liveness
- Не сертифицированная модель

**Целевое состояние:**
- Silent-Face-Anti-Spoofing или MiniFASNetV2
- Точность >95%, ложные отказы <3%

**Шаги:**
1. Установить зависимости:
   ```bash
   pip install silent-face-anti-spoofing
   # или
   pip install MiniFASNetV2
   ```

2. Интегрировать модель:
   ```python
   from silent_face_anti_spoofing import AntiSpoofing
   
   anti_spoof = AntiSpoofing()
   result = anti_spoof.predict(image)
   ```

3. Заменить эвристическую реализацию:
   - Обновить `check_liveness()` в MLService
   - Использовать сертифицированную модель
   - Сохранить fallback на эвристику

4. Калибровка:
   - Тестирование на наборе атак
   - Калибровка порогов
   - Достижение целевых метрик

**Файлы для изменения:**
- `app/services/ml_service.py` - метод `check_liveness()`
- `app/services/ml_service.py` - метод `_heuristic_liveness_check()` (заменить)
- `requirements.txt` - добавить зависимость

---

## 📋 ЭТАП 3: Переход на AES-256-GCM (1 неделя)

### 3.1 Реализация AES-256-GCM

**Текущее состояние:**
- Fernet (AES-128)
- `EncryptionService` использует Fernet

**Целевое состояние:**
- AES-256-GCM
- Совместимость с существующими данными

**Шаги:**
1. Реализовать AES-256-GCM:
   ```python
   from cryptography.hazmat.primitives.ciphers.aead import AESGCM
   from cryptography.hazmat.primitives import hashes
   from cryptography.hazmat.primitives.kdf.pbkdf2 import PBKDF2HMAC
   
   # Генерация ключа
   kdf = PBKDF2HMAC(
       algorithm=hashes.SHA256(),
       length=32,
       salt=salt,
       iterations=100000,
   )
   key = kdf.derive(password)
   
   # Шифрование
   aesgcm = AESGCM(key)
   nonce = os.urandom(12)
   ciphertext = aesgcm.encrypt(nonce, data, associated_data)
   ```

2. Обновить `EncryptionService`:
   - Добавить метод `encrypt_aes256gcm()`
   - Добавить метод `decrypt_aes256gcm()`
   - Сохранить обратную совместимость с Fernet

3. Миграция данных:
   - Скрипт миграции существующих данных
   - Двойное шифрование (старое + новое) на период миграции
   - Постепенный переход

4. Тестирование:
   - Проверить шифрование/расшифровку
   - Проверить производительность
   - Проверить совместимость

**Файлы для изменения:**
- `app/services/encryption_service.py` - добавить AES-256-GCM методы
- Создать `scripts/migrate_encryption.py` - скрипт миграции

---

## 📋 ЭТАП 4: Настройка очередей (1 неделя)

### 4.1 Redis Streams или RabbitMQ

**Текущее состояние:**
- Celery/Dramatiq есть, но нужно проверить использование

**Целевое состояние:**
- Redis Streams или RabbitMQ
- Явное использование для асинхронных задач

**Шаги:**
1. Выбрать решение:
   - Redis Streams (проще, уже есть Redis)
   - RabbitMQ (более функционально)

2. Настроить очередь:
   ```python
   # Redis Streams пример
   import redis.asyncio as redis
   
   async def publish_task(stream_name, task_data):
       r = await redis.from_url(settings.REDIS_URL)
       await r.xadd(stream_name, task_data)
   
   async def consume_tasks(stream_name):
       r = await redis.from_url(settings.REDIS_URL)
       while True:
           messages = await r.xread({stream_name: '$'}, block=1000)
           for msg in messages:
               await process_task(msg)
   ```

3. Интегрировать в Processing Orchestrator:
   - Создать `ProcessingOrchestrator` сервис
   - Настроить очереди для разных типов задач
   - Реализовать retries с exponential backoff

4. Обновить endpoints:
   - `/verify` - синхронный (оставить)
   - `/liveness` (активная) - асинхронный через очередь
   - `/update-reference` - асинхронный через очередь

**Файлы для создания:**
- `app/services/processing_orchestrator.py` - новый сервис
- `app/services/queue_service.py` - обертка над очередью

---

## 📋 ЭТАП 5: Добавление pgvector/FAISS (1 неделя)

### 5.1 Выбор решения

**Для баз < 100k:**
- pgvector (расширение PostgreSQL)

**Для баз > 1 млн:**
- FAISS (отдельный сервис)

**Рекомендация:** Начать с pgvector, добавить FAISS при необходимости

### 5.2 Реализация pgvector

**Шаги:**
1. Установить pgvector:
   ```sql
   CREATE EXTENSION vector;
   ```

2. Обновить модель Reference:
   ```python
   from pgvector.sqlalchemy import Vector
   
   class Reference(Base):
       embedding_vector = Column(Vector(512))  # pgvector колонка
   ```

3. Обновить CRUD:
   ```python
   # Поиск похожих эталонов
   stmt = select(Reference).order_by(
       Reference.embedding_vector.cosine_distance(query_vector)
   ).limit(10)
   ```

4. Миграция:
   - Создать Alembic миграцию
   - Конвертировать существующие эмбеддинги в векторный формат

**Файлы для изменения:**
- `app/db/models.py` - добавить Vector колонку
- `alembic/versions/003_add_pgvector.py` - миграция
- `app/db/crud.py` - обновить поиск

---

## 📋 ЭТАП 6: OpenTelemetry трассировка (3-5 дней)

### 6.1 Интеграция OpenTelemetry

**Шаги:**
1. Установить зависимости:
   ```bash
   pip install opentelemetry-api
   pip install opentelemetry-sdk
   pip install opentelemetry-instrumentation-fastapi
   pip install opentelemetry-exporter-jaeger
   ```

2. Настроить трассировку:
   ```python
   from opentelemetry import trace
   from opentelemetry.sdk.trace import TracerProvider
   from opentelemetry.sdk.trace.export import BatchSpanProcessor
   from opentelemetry.exporter.jaeger import JaegerExporter
   
   trace.set_tracer_provider(TracerProvider())
   tracer = trace.get_tracer(__name__)
   
   jaeger_exporter = JaegerExporter(
       agent_host_name="localhost",
       agent_port=6831,
   )
   trace.get_tracer_provider().add_span_processor(
       BatchSpanProcessor(jaeger_exporter)
   )
   ```

3. Инструментировать код:
   - Добавить spans в ключевые операции
   - Трассировка ML пайплайна
   - Трассировка БД операций

4. Подключить к мониторингу:
   - Jaeger для визуализации
   - Интеграция с Grafana

**Файлы для изменения:**
- `app/main.py` - инициализация OpenTelemetry
- `app/services/ml_service.py` - добавить spans
- `app/db/crud.py` - добавить spans

---

## 📋 ЭТАП 7: Улучшение QC (3-5 дней)

### 7.1 Полноценный Occlusion Detection

**Текущее состояние:**
- Базовая проверка наличия лица

**Целевое состояние:**
- Детекция масок, очков VR, окклюзий

**Шаги:**
1. Добавить детекцию ключевых точек:
   ```python
   # Использовать landmarks для детекции окклюзий
   landmarks = detector.get_landmarks(face)
   
   # Проверка окклюзий
   occlusion_score = detect_occlusions(landmarks, image)
   ```

2. Интегрировать в ValidationService:
   - Обновить `_assess_image_quality()`
   - Добавить проверку окклюзий
   - Вернуть детальную диагностику

**Файлы для изменения:**
- `app/services/validation_service.py` - улучшить QC

---

## 📋 ЭТАП 8: A/B тестирование порогов (1 неделя)

### 8.1 Реализация A/B конфига

**Шаги:**
1. Создать систему A/B тестирования:
   ```python
   class ThresholdConfig:
       def get_threshold(self, user_id: str, quality: float) -> float:
           # A/B логика
           variant = self.get_variant(user_id)
           if variant == "A":
               return self.calculate_threshold_a(quality)
           else:
               return self.calculate_threshold_b(quality)
   ```

2. Интегрировать в verify endpoint:
   - Использовать A/B конфиг
   - Логировать результаты
   - Собирать метрики

3. Автоматическая калибровка:
   - ROC/DET кривые
   - Оптимизация порогов
   - Обратная связь

**Файлы для создания:**
- `app/services/threshold_service.py` - новый сервис
- `app/utils/roc_analyzer.py` - анализ ROC кривых

---

## 📊 ПРИОРИТИЗАЦИЯ

### Критично (сделать в первую очередь):
1. ✅ Интеграция SCRFD и ArcFace
2. ✅ Интеграция Silent-Face-Anti-Spoofing
3. ✅ Переход на AES-256-GCM

### Важно (сделать во вторую очередь):
4. ✅ Настройка очередей
5. ✅ Добавление pgvector/FAISS
6. ✅ OpenTelemetry трассировка

### Желательно (можно отложить):
7. Улучшение QC (occlusion detection)
8. A/B тестирование порогов
9. Kubernetes манифесты

---

## ⏱️ ОЦЕНКА ВРЕМЕНИ

- **Этап 1:** 2-3 недели (критично)
- **Этап 2:** 1-2 недели (критично)
- **Этап 3:** 1 неделя (критично)
- **Этап 4:** 1 неделя (важно)
- **Этап 5:** 1 неделя (важно)
- **Этап 6:** 3-5 дней (важно)
- **Этап 7:** 3-5 дней (желательно)
- **Этап 8:** 1 неделя (желательно)

**Итого:** 6-8 недель для критичных и важных этапов

---

## 🚀 НАЧАЛО РАБОТЫ

### Можно начинать разработку сейчас:
- ✅ Базовая функциональность работает
- ✅ Архитектура правильная
- ✅ Безопасность данных обеспечена

### Параллельно планировать миграцию:
- ⚠️ Этап 1-3 (критичные изменения)
- ⚠️ Этап 4-6 (важные улучшения)

### Рекомендация:
Начать разработку с текущим стеком (MTCNN/FaceNet), параллельно работая над миграцией на SCRFD/ArcFace. Это позволит не блокировать разработку и постепенно улучшать систему.


